// Models.swift (shim)
// Intentionally empty because @Model types (PaySchedule, IncomeSource, Bill)
// and enums (PayFrequency, BillRecurrence) are already defined elsewhere
// in this target. Keeping this file prevents missing-file refs without
// causing “Invalid redeclaration” errors.

import Foundation
import SwiftData

// No declarations here on purpose.

